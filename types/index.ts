export type Role = "ADMIN" | "MANAGER" | "VIEWER";
export type Status = "TODO" | "IN_PROGRESS" | "WAITING" | "COMPLETED" | "CANCELLED";
export type Priority = "NORMAL" | "IMPORTANT" | "URGENT";
export type User = { id:string; name:string; email:string; role:Role; color:string; active:boolean; canManageEvents:boolean; passwordHash?:string };
export type ChecklistItem = { id:string; text:string; done:boolean };
export type Audit = { id:string; at:string; actor:string; action:string; detail?:string };
export type RecurrenceAnchor = "DUE_DATE" | "COMPLETION_DATE";
export type WorkingDayAdjustment = "NONE" | "PREVIOUS_WORKDAY" | "NEXT_WORKDAY";
export type Deadline = { id:string; title:string; description:string; dueDate:string; startDate?:string; endDate?:string; datePrecision?: "DAY"|"MONTH"|"YEAR"; dueTime?:string; priority:Priority; status:Status; category:string; assigneeId:string; notifyIds:string[]; recurrence:string; recurrenceAnchor?:RecurrenceAnchor; workingDayAdjustment?:WorkingDayAdjustment; dependsOnIds?:string[]; reminders:number[]; notes:string; waitingFor:string; requireRead:boolean; checklist:ChecklistItem[]; links:string[]; createdAt:string; updatedAt:string; takenAt?:string; completedAt?:string; deletedAt?:string; archivedAt?:string; archiveExempt?:boolean; audits:Audit[] };
export type Database = { users:User[]; deadlines:Deadline[]; categories:string[]; archiveAfterDays?:number; notificationLog:{id:string;message:string;at:string}[] };
