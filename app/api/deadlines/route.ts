import { NextResponse } from "next/server";import { getSession } from "@/lib/auth";import { readDb,updateDb } from "@/lib/store";import { z } from "zod";
const schema=z.object({title:z.string().min(2),dueDate:z.string(),priority:z.enum(["NORMAL","IMPORTANT","URGENT"]),assigneeId:z.string(),description:z.string().default(""),category:z.string().default("Altro"),recurrence:z.string().default("NONE"),notes:z.string().default(""),waitingFor:z.string().default(""),requireRead:z.boolean().default(false),links:z.array(z.string()).default([]),checklist:z.array(z.object({id:z.string(),text:z.string(),done:z.boolean()})).default([]),notifyIds:z.array(z.string()).default([]),reminders:z.array(z.number()).default([])});
export async function GET(){const s=await getSession();if(!s)return NextResponse.json({error:"Non autorizzato"},{status:401});const db=await readDb();return NextResponse.json(db)}
export async function POST(req:Request){const s=await getSession();if(!s||s.role==="VIEWER")return NextResponse.json({error:"Non autorizzato"},{status:403});const p=schema.parse(await req.json());return NextResponse.json(await updateDb(db=>{const now=new Date().toISOString();const item={...p,id:crypto.randomUUID(),status:"TODO" as const,dueTime:undefined,createdAt:now,updatedAt:now,audits:[{id:crypto.randomUUID(),at:now,actor:s.name,action:"Creazione"}]};db.deadlines.push(item);return item}),{status:201})}
