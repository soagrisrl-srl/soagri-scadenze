"use client";import { useState } from "react";import { Leaf,Lock,ArrowRight } from "lucide-react";
export function Login(){const [email,setEmail]=useState("domenico@soagri.local"),[password,setPassword]=useState("soagri2026"),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(r.ok)location.reload();else{setError("Email o password non corretti");setBusy(false)}}return <main className="login-page"><div className="login-brand"><span className="brand-mark"><Leaf/></span><div><b>SO.AGRI</b><small>Scadenze</small></div></div><section className="login-card"><div className="login-intro"><span className="eyebrow">AREA RISERVATA</span><h1>Buon lavoro.</h1><p>Accedi per vedere subito ciò che richiede attenzione.</p></div><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label>Password<div className="input-icon"><Lock size={18}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></div></label>{error&&<p className="form-error">{error}</p>}<button className="primary wide" disabled={busy}>{busy?"Accesso…":"Accedi"}<ArrowRight size={18}/></button></form><p className="demo-note">Demo locale: Domenico · password <code>soagri2026</code></p></section></main>}
